PORT=3001
# Optional: use Python AI service (OpenAI + Anthropic + Gemini). Start ai-service first.
# PYTHON_AI_URL=http://localhost:5000
# Optional: fallback if not using Python AI (Node-only OpenAI or canned tips)
# OPENAI_API_KEY=sk-...
